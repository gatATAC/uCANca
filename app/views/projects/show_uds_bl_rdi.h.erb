#ifndef UDS_BL_RDI_GEN_H
#define UDS_BL_RDI_GEN_H

...

<%= ret=@code
    raw(ret) %>

/* Number of RDI: <%= raw(@number) %> */

/* Memory definitions */
<%= raw(@code_mem) %>

void UdsRdiInit(void);

#define UDS_BL_RDI_REDIRECT_APP_RDI

#ifndef UDS_BL_RDI_REDIRECT_APP_RDI

<%= ret=@code_func
    raw(ret) %>

#else 

#include "UdsRdiGen.h"

/*********** BOOTLOADER SECTION *****************/
<%= ret=@code_redirect
    raw(ret) %>
#endif

#endif /* UDS_RDI_H_ */

