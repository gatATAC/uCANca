#ifndef UDS_BL_SERVICES_GEN_H
#define	UDS_BL_SERVICES_GEN_H

...
#include "UDServices.h"

/* This header holds all the services that do not have a sub service */

<%= ret=@code
    raw(ret) %>

/* Number of Services: <%= raw(@number) %> */

#define UDS_SERV_NUMBER <%= raw(@number) %>

#endif /* UDS_BL_SERVICES_GEN_H */
