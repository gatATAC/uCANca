/************ UdsBlWdiGen.c **********/
...
#include "UdsBlWdiGen.h"
#include "UDServices.h"

#ifdef UDS_GEN_USE_AUTOGEN_WDI
<%= raw(@code) %>
#endif

/**
 * Initialization of the WDI services
 */
void UdsWdiInit(void)
{
}

/**
 * Dispatcher of the WDI services
 */
void UdsWriteDataByIdentifier(...)
{
...
    switch (id) {
<%= raw(@code_switch) %>
    default:
...
        break;
    }
...
}
