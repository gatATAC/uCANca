#ifndef UDS_IOCTL_GEN_H
#define UDS_IOCTL_GEN_H

...
#include "UDServices.h"
<%= ret=@code
    raw(ret) %>
/* Number of IOCTL: <%= raw(@number) %> */

/**
 * Initialization of the IOCtrl manager
 */
void UdsIoCtlInit(void);

/* The UDS_GEN_USE_AUTOGEN_IOCTL definition allows the use of automatically generated routines
    this can be useful for initial integration of code
 */
#ifndef UDS_GEN_USE_AUTOGEN_IOCTL
<%= ret=@code_func
    raw(ret) %>
#endif

#endif /* UDS_IOCTL_H_ */

