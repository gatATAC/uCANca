/********** UdsRdiGen.c *************/
...
#include "UDServices.h"
#include "UdsRdiGen.h"

#ifdef UDS_GEN_USE_AUTOGEN_RDI
<%= raw(@code) %>

#endif

/**
 * Initialization of the UDS RDI services
 */
void UdsRdiInit(void)
{
}

/**
 * Read Data by Identifier dispatcher
 */
uint8_t UdsRdiDispatcher(...)
{
...
      switch (id) {
        
<%= raw(@code_switch) %>
        default:
...
          break;
        }

...
}
