#ifndef UDS_WDI_GEN_H
#define UDS_WDI_GEN_H

...
#include "UdsRdiGen.h"
<%= ret=@code
    raw(ret) %>

/* Number of WDI: <%= raw(@number) %> */
#define UDS_WDI_NUMBER <%= raw(@number) %>

/* Memory definitions, translation to RDI when it exists */
<%= raw(@code_mem) %>

/**
 * Initialization of the WDI dispatcher
 */
void UdsWdiInit(void);

/* Using UDS_GEN_USE_AUTOGEN_WDI definition, the automatically generated 
   WDI functions will be compiled.  This can be useful at the early stages 
   before manual code is implemented. */
#ifndef UDS_GEN_USE_AUTOGEN_WDI
<%= ret=@code_decl
    raw(ret) %>

#endif

#endif /* UDS_WDI_H_ */

