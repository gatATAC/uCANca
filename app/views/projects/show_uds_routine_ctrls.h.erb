#ifndef UDS_ROUTINE_CTRLS_GEN_H
#define UDS_ROUTINE_CTRLS_GEN_H

...
#include "UDServices.h"

<%= ret=@code
    raw(ret) %>

<%= ret=@code_func
    raw(ret) %>

/**
 * Initializes the RoutineCtrl dispatcher
 */
void UdsRoutineCtrlsInit(void);

#endif /* UDS_ROUTINE_CTRLS_GEN_H_ */
