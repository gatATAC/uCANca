/******** UdsIoCtlGen.c *********/
/* Example of code for implementing IOCtrl in application */
...
#include "UDServices.h"
#include "UdsIoctlGen.h"

/* #define UDS_GEN_USE_AUTOGEN_IOCTL */
#ifdef UDS_GEN_USE_AUTOGEN_IOCTL

<%= raw(@code) %>

#endif


/**
 * Initialization of the UDS IOCtrl services
 */
void UdsIoCtlInit(void)
{

}

void UdsIoCtlDispatcher(...)
{
...
        switch ((uint16_t) ident) {
<%= raw(@code_switch) %>
        default:
...          
        break;
        }
    }
...
}

