#ifndef UDS_BL_WDI_GEN_H
#define UDS_BL_WDI_GEN_H

/* TO DO: use a project preference to store this pattern in the BD */
/* Include here the needed *.h 
...
...
...
...

*/
#include "uds_bl_rdi_gen.h"


<%= ret=@code
    raw(ret) %>

/* Number of WDI: <%= raw(@number) %> */

/* Memory definitions */
<%= raw(@code_mem) %>

#define UDS_BL_WDI_NUMBER <%= raw(@number) %>

void UdsWdiInit(void);
uint8_t UdsWdiDispatcher(uint16_t ident, uint8_t *buf_data_rx, uint16_t size, t_wdi_resp *resp);

#endif /* UDS_BL_WDI_H_ */

